// This file is deprecated. Using next.config.js instead.
// Keeping this file to avoid deletion issues.

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
